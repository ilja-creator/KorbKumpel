document.addEventListener("DOMContentLoaded", () => {
    const newListButton = document.getElementById("news_list");

    newListButton.addEventListener("click", () => {
        console.log("Button clicked!");
    });
});